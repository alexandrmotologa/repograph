# RepoGraph

RepoGraph is a local-first codebase knowledge graph and blast-radius calculation engine. It parses Python, TypeScript/JavaScript, and Java repositories using Tree-sitter, constructs a directed code property graph with NetworkX, tracks upstream callers and downstream side-effects, detects circular dependencies and dead code, and provides terminal and web interfaces.
